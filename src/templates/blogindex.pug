extends layout

block content
	h2 Blog

	each post in data.reverse()
		unless post.destination === '/'
			div
				span!= new Date(post.date).toLocaleDateString() + ' &mdash; '
				a(href="/blog/" + post.destination)
					h3(style='display:inline')= post.title
